<Page x:Class="SpatialTileBuilder.App.Views.ShellPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="1400"
      Background="{DynamicResource MaterialDesignPaper}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top App Bar -->
        <materialDesign:ColorZone Mode="PrimaryMid" 
                                 Padding="16" 
                                 materialDesign:ElevationAssist.Elevation="Dp4">
            <DockPanel>
                <TextBlock Text="Spatial Tile Builder" 
                          VerticalAlignment="Center" 
                          FontSize="20" 
                          FontWeight="Medium"/>
            </DockPanel>
        </materialDesign:ColorZone>

        <!-- Navigation Rail + Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation Menu -->
            <Border Grid.Column="0" 
                   Background="{DynamicResource MaterialDesignCardBackground}"
                   BorderBrush="{DynamicResource MaterialDesignDivider}"
                   BorderThickness="0,0,1,0">
                <ListBox x:Name="NavListBox" 
                        SelectionChanged="NavListBox_SelectionChanged"
                        Margin="0,8">
                    <ListBox.Resources>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Height" Value="56"/>
                            <Setter Property="Padding" Value="16,0"/>
                        </Style>
                    </ListBox.Resources>

                    <ListBoxItem Tag="ConnectionWizardPage">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Globe" Width="24" Height="24" VerticalAlignment="Center"/>
                            <TextBlock Text="Connection" VerticalAlignment="Center" Margin="16,0,0,0"/>
                        </StackPanel>
                    </ListBoxItem>

                    <ListBoxItem Tag="LayerSelectionPage">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Map" Width="24" Height="24" VerticalAlignment="Center"/>
                            <TextBlock Text="Select Layers" VerticalAlignment="Center" Margin="16,0,0,0"/>
                        </StackPanel>
                    </ListBoxItem>

                    <ListBoxItem Tag="StylePreviewPage">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Palette" Width="24" Height="24" VerticalAlignment="Center"/>
                            <TextBlock Text="Style Editor" VerticalAlignment="Center" Margin="16,0,0,0"/>
                        </StackPanel>
                    </ListBoxItem>

                    <ListBoxItem Tag="RegionSelectionPage">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Crop" Width="24" Height="24" VerticalAlignment="Center"/>
                            <TextBlock Text="Set Region" VerticalAlignment="Center" Margin="16,0,0,0"/>
                        </StackPanel>
                    </ListBoxItem>

                    <ListBoxItem Tag="GenerationMonitorPage">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Play" Width="24" Height="24" VerticalAlignment="Center"/>
                            <TextBlock Text="Build Tiles" VerticalAlignment="Center" Margin="16,0,0,0"/>
                        </StackPanel>
                    </ListBoxItem>

                    <Separator Style="{DynamicResource MaterialDesignSeparator}" Margin="0,8"/>

                    <ListBoxItem Tag="SettingsPage">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Settings" Width="24" Height="24" VerticalAlignment="Center"/>
                            <TextBlock Text="Settings" VerticalAlignment="Center" Margin="16,0,0,0"/>
                        </StackPanel>
                    </ListBoxItem>
                </ListBox>
            </Border>

            <!-- Content Frame -->
            <Frame x:Name="ContentFrame" 
                  Grid.Column="1" 
                  NavigationUIVisibility="Hidden"
                  Margin="8"/>
        </Grid>
    </Grid>
</Page>
