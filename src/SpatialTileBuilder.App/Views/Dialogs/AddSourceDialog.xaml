<Window x:Class="SpatialTileBuilder.App.Views.Dialogs.AddSourceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:SpatialTileBuilder.App.ViewModels.Dialogs"
        mc:Ignorable="d"
        Title="Add Data Source" Height="450" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Add Data Source" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,16"/>

        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}" Style="{StaticResource MaterialDesignTabControl}">
            <TabItem Header="PostGIS">
                <StackPanel Margin="0,16,0,0">
                    <TextBox materialDesign:HintAssist.Hint="Display Name (Optional)" Text="{Binding SourceName}" Margin="0,0,0,8"/>
                    <TextBox materialDesign:HintAssist.Hint="Host" Text="{Binding Host}" Margin="0,0,0,8"/>
                    <TextBox materialDesign:HintAssist.Hint="Port" Text="{Binding Port}" Margin="0,0,0,8"/>
                    <TextBox materialDesign:HintAssist.Hint="Database" Text="{Binding Database}" Margin="0,0,0,8"/>
                    <TextBox materialDesign:HintAssist.Hint="Username" Text="{Binding Username}" Margin="0,0,0,8"/>
                    <PasswordBox materialDesign:HintAssist.Hint="Password" x:Name="PasswordBox" Margin="0,0,0,8"/> 
                    <!-- Password binding needs helper or code behind, simpler to skip specific binding for now or use attached property -->
                    
                    <ComboBox materialDesign:HintAssist.Hint="SSL Mode" Text="{Binding SslMode}">
                        <ComboBoxItem Content="Disable"/>
                        <ComboBoxItem Content="Require"/>
                    </ComboBox>
                </StackPanel>
            </TabItem>
            <TabItem Header="File">
                <StackPanel Margin="0,16,0,0">
                    <TextBox materialDesign:HintAssist.Hint="Display Name (Optional)" Text="{Binding SourceName}" Margin="0,0,0,16"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox materialDesign:HintAssist.Hint="File Path" Text="{Binding FilePath}" IsReadOnly="True"/>
                        <Button Grid.Column="1" Content="..." Command="{Binding BrowseFileCommand}" Margin="8,0,0,0"/>
                    </Grid>
                </StackPanel>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,8,0"/>
            <Button Content="Add Source" Command="{Binding AddCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
