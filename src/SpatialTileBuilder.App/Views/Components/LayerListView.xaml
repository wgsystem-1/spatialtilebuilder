<UserControl x:Class="SpatialTileBuilder.App.Views.Components.LayerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpatialTileBuilder.App.Views.Components"
             xmlns:vm="clr-namespace:SpatialTileBuilder.App.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ToolBarTray Grid.Row="0">
            <ToolBar Style="{DynamicResource MaterialDesignToolBar}" ClipToBounds="False">
                <Button ToolTip="Move Up" Command="{Binding MoveLayerUpCommand}">
                    <materialDesign:PackIcon Kind="ArrowUp"/>
                </Button>
                <Button ToolTip="Move Down" Command="{Binding MoveLayerDownCommand}">
                    <materialDesign:PackIcon Kind="ArrowDown"/>
                </Button>
                <Button ToolTip="Remove Layer" Command="{Binding RemoveLayerCommand}">
                    <materialDesign:PackIcon Kind="Delete"/>
                </Button>
            </ToolBar>
        </ToolBarTray>

        <ListView Grid.Row="1" ItemsSource="{Binding Layers}" SelectedItem="{Binding SelectedLayer}" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsVisible}" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
